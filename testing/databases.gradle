// Note: The JDBC URL must be configured properly to your GCP Project settings.
ext {
        db = 'spanner'
        dbBundle = [
                spanner : [
                        'db.dialect' : 'knut.dialect.CloudSpannerDialect',
                        'jdbc.driver' : 'com.simba.cloudspanner.core.jdbc42.CloudSpanner42Driver',
                        'jdbc.user' : '',
                        'jdbc.pass' : '',
                        'jdbc.url' : 'jdbc:cloudspanner://;Project=my-kubernetes-codelab-217414;Instance=spring-demo;Database=experiments',
                ],
        ]
}
